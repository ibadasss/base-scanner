# Optional: Etherscan V2 multichain API key (works for Base, chainid=8453).
# Used only to enrich the safety layer with contract-verification checks.
# The screener runs fine WITHOUT this key using free DefiLlama + GeckoTerminal data.
ETHERSCAN_API_KEY=
